name: 'build-test'
on: # rebuild any PRs and main branch changes
  pull_request:
  push:
    branches:
      - main
      - 'releases/*'

jobs:
  build: # make sure build/ci work properly
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v2
      - run: |
          npm install
      - run: |
          npm run all
  test: # make sure the action works on a clean machine without building
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v2
      - run: |
          cd DemoSwiftPackage 
          swift build --target DemoSwiftPackage -Xswiftc -emit-symbol-graph -Xswiftc -emit-symbol-graph-dir -Xswiftc .build
      - uses: ./
        with:
          path: DemoSwiftPackage/Sources/DemoSwiftPackage/DemoSwiftPackage.docc
          additional-symbol-graph-dir: DemoSwiftPackage/.build

